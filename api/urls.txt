/user ? POST auth=pwd // authenticate
/object ? // listall
/object ? POST {id:"id"} // list one
/object ? POST { data:{} } // insert
/object ? POST { id:"id", data:{} } // update


/usuari ? POST {"pwd":""} // autenticar
/usuari/logout // desautenticar
/noticia // llista totes les noticies (les primeres 20)
/noticia/short // llista totes les noticies (les primeres 20) tallant camps llargs
/noticia/p/1 // torna la segona pagina de noticies (les segones 20)
/noticia ? POST {elements} // nova noticia
/noticia/id/{id} // torna una noticia amb eixe id
/noticia/id/{id} ? POST { elements} // update noticia
/noticia/c/{categoria} // torna totes les noticies que incloguen la categoria
/noticia/t/{tag} // torna totes les noticies que incloguen el tag
/noticia/u/{usuari} // torna totes les noticies d'un usuari
/noticia/d/{20170524} // torna totes les noticie d'una data

/noticia/p/4/u/32

- el mateix canviant noticia per pagina



temporada
competicions
modalitat
club
jugadors
partida - resultat

Estic definint el model de dades per a començar a fer proves i introduir informació per veure si podem arrimar-nos al que esteu fent actualment. Propose el següents conceptes:

Es defineix un club com una entitat amb un nom.
Es defineix un equip com una entitat que pertany a un club i té una modalitat específica.
Es defineix un jugador com una entitat amb nom, dni, numero de soci, data naixement, domicili, foto, ...
Es defineix una temporada indicant data d'inici i de final. Tota competició farà referència a una temporada.
Es defineix una modalitat com un sistema de joc amb les seues regles. Tota partida, tot equip, i tota competició seran d'una modalitat específica.
Es defineix una competició com a un conjunt de partides.
Es defineix una partida com una entitat amb un dia determinat, amb dos equips indicats, amb una sèrie de jugadors i amb un resultat.
Definiré un sistema de vinculació entre equips i jugadors de forma que quan un jugador participe en una partida, ho farà amb un equip i, per tant, s'utilitzarà eixa informació per donar prioritat a eixe jugador en la llista de possibles jugadors d'eixe equip, per tal d'agilitzar la selecció.